pluginName = "nr_aws" //needs to match the name used in plugin serverConfig

pluginCmd = "plugin/server/noderesolver-aws"
pluginChecksum = ""
enabled = true
pluginType = "NodeResolver" //needs to match the handshake
pluginData {
	trust_domain = "example.org"
}

